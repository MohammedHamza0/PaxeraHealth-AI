# Backend Configuration
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BACKEND_RELOAD=true

# Frontend Configuration (backend serves frontend at this URL)
FRONTEND_URL=http://127.0.0.1:8000

# Model Configuration
MODEL_DIR=models
BINARY_MODEL_PATH=best_unet_binary_scratch.keras
MULTI_MODEL_PATH=best_unet_multi_scratch.keras

# Inference Configuration
INFERENCE_TARGET_SIZE=256
INFERENCE_CONFIDENCE_THRESHOLD=0.5
